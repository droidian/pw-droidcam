context.spa-libs = {
  droidcamsrc = droidcam/libspa-droidcam
}

context.objects = [
  { factory = spa-node-factory
    args = {
      factory.name = droidcamsrc
      node.name = "Droid Camera"
      node.description = "Camera using hybris compat layer"
    }
  }
]
